# 基础循迹低速

基础低速循迹代码快照，基础 PWM 为 `1500`。

本版本假设八路灰度传感器安装在车头：D1 位于车身左侧，D8 位于车身右侧。车辆正向行驶；普通循迹、急弯、丢线找线和固定航向转弯均已使用对应方向。

检测到直角急弯后，控制器立即进入左右轮差速转向，不再先执行刹车等待。急弯时内侧轮立即为 `0`，外侧轮为 `1850`，以增大入弯差速。

`user/` 和 `code/` 包含本版本的应用源代码。构建时复用仓库根目录已有的 `ml_libs/` 与 `m0_sdk/` 公共依赖。
